# HearthVale

HearthVale is a Living World Engine Shell.

This repository contains the clean LWE-based implementation.

The previous pre-LWE implementation is preserved separately in:
../HearthVale_Legacy_PreLWE

The legacy implementation is reference-only and must not be used as a runtime dependency.
